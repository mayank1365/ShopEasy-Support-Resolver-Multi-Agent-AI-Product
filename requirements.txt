# Multi-Agent E-commerce Support Resolver — dependencies
langgraph>=0.2.0          # graph orchestration: state, nodes, edges, routing, HITL
langchain-core>=0.3.0     # required by langgraph
anthropic>=0.40.0         # Claude API (claude-opus-4-8)
scikit-learn>=1.3.0       # TF-IDF retriever for RAG over help-docs
streamlit>=1.30.0         # demo UI with the human approval gate
python-dotenv>=1.0.0      # loads ANTHROPIC_API_KEY from .env
pydantic>=2.0.0           # structured agent outputs / handoffs
langsmith>=0.1.0          # observability: traces every agent + LLM call
notebook>=7.0.0           # the walkthrough.ipynb explainer notebook
grandalf>=0.8             # ASCII graph rendering fallback for the notebook
